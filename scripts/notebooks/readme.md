# Notebooks

Store exploratory and presentation notebooks here. Notebooks may read from
`data/bronze` and `data/silver`, but reusable transformations belong in the
`src` package rather than notebook cells.
